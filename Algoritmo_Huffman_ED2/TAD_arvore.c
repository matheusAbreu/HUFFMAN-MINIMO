#include <string.h>
#include 'TAD_arvore.h'
No_cego analiseFrequencia(char *palavras )
{
    char *copia, verif;
    int i,a,j;
    controle *fila = malloc (sizeof(controle));

    a = strlen(palavras);
    copia= (char*) malloc (a * sizeof(char));
    strlen(copia, palavras);

    verif= copia[0];

    for(i=0;i<=a ;i++ )
    {
       for(j=i; j <= a; j++)
       {
           if(i==)
       }
    }
}
